I'm lost on this project atm. Need to better than just taking notes on what happened. Should put goals, results, and plan for next session whether for me or claude.
Will switch back to tickets in a bit I think but really just want to get through this next section of refactor and testing. 

Today will try to add deactivate (and reactivate logic) for as many data types as possible. Will probably not finish, but if I do still need to add hard deletes.
after that check feb2/4th for other bugs.


Used Claude to refactor when got tired of the renaming. probably should have updated tests first as that did get into behavior.
meant to limit it to renaming. But a little more than that happened.

Still think most of the logic needs to be implemented
Tried to run as a docker in docker dev container but couldn't install my libraries. probably want custom image.
Claude broke a good 

